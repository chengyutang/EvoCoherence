// import Node;
import java.util.HashMap;

public class CoherenceNetwork {
	public static void main(String args[]) {
		// Node a = new Node("node_a", 0.5);
		// Node b = new Node("node_a", 0.2);
		// Node c = new Node("node_c", 0.3);
		// a.addNeighbor(b, 0.4);
		// a.addNeighbor(c, 0.8, false);
		// // HashMap<Node, Double> neighbor = a.getNeighbors();
		// System.out.println(a.getNeighbors().containsKey(b));
		// System.out.println(a.getNeighbors().containsKey(c));
		// System.out.println(b.getNeighbors().containsKey(a));
		// System.out.println(c.getNeighbors().containsKey(a));
		Node b1 = new Node("B1", "The available data are unclear about whether evolution actually occurs.")
	}
}